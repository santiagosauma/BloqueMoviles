<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Agregar permisos de Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClinicaPenal"
        tools:targetApi="31">

        <!-- Declaración de MainActivity -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ClinicaPenal">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de FullscreenActivity, esta debe estar separada -->
        <activity
            android:name=".view.videoPlayer.fullscreenActivities.FullscreenActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            tools:ignore="DiscouragedApi,MissingClass" /> <!-- Puedes definir el valor de `exported` según tus necesidades -->
        <activity
            android:name=".view.videoPlayer.fullscreenActivities.FullscreenVideoActivity"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            tools:ignore="DiscouragedApi,MissingClass" />

        <service
            android:name=".notificaciones.FirebaseClass"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/logoapp" />
    </application>


</manifest>